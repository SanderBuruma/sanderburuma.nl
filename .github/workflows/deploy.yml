name: Deploy to VPS

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Deploy to VPS
        uses: appleboy/ssh-action@v1.0.0
        with:
          host: 45.76.20.122
          username: root
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            /root/deploy.sh

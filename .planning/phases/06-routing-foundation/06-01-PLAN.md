---
phase: 06-routing-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - src/App.jsx
  - src/pages/Portfolio.jsx
autonomous: true

must_haves:
  truths:
    - "React Router is installed and available for import"
    - "App renders different content based on URL path"
    - "Portfolio sections remain intact at root path"
  artifacts:
    - path: "package.json"
      provides: "react-router-dom dependency"
      contains: "react-router-dom"
    - path: "src/App.jsx"
      provides: "BrowserRouter and Routes setup"
      contains: "BrowserRouter"
    - path: "src/pages/Portfolio.jsx"
      provides: "Portfolio page with all existing sections"
      min_lines: 30
  key_links:
    - from: "src/App.jsx"
      to: "react-router-dom"
      via: "import statement"
      pattern: "import.*from.*react-router-dom"
    - from: "src/App.jsx"
      to: "src/pages/Portfolio.jsx"
      via: "Route element"
      pattern: "element=\\{<Portfolio"
---

<objective>
Install React Router and set up the foundational routing structure that enables blog routes while preserving the existing portfolio.

Purpose: Establishes the routing infrastructure that all other phase 6 work depends on. Without this foundation, navigation between portfolio and blog is impossible.

Output: Working router setup with portfolio content at `/` and route placeholders for `/blog` and `/blog/:slug`.
</objective>

<execution_context>
@/home/sanderburuma/.claude/get-shit-done/workflows/execute-plan.md
@/home/sanderburuma/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-routing-foundation/06-CONTEXT.md
@src/App.jsx
@src/components/Navbar.jsx
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install React Router</name>
  <files>package.json, package-lock.json</files>
  <action>
    Run `npm install react-router-dom` to add React Router v7 (latest stable).

    Do NOT use HashRouter (requires #/blog URLs which look ugly).
    Use BrowserRouter for clean URLs (/blog, /blog/my-post).
  </action>
  <verify>
    Run `npm ls react-router-dom` to confirm installation.
    Run `npm run build` to verify no dependency conflicts.
  </verify>
  <done>react-router-dom appears in package.json dependencies and project builds successfully.</done>
</task>

<task type="auto">
  <name>Task 2: Extract Portfolio page and set up router structure</name>
  <files>src/App.jsx, src/pages/Portfolio.jsx</files>
  <action>
    1. Create `src/pages/` directory.

    2. Create `src/pages/Portfolio.jsx`:
       - Move AppContent's section composition (Navbar through Footer) into this component
       - Move the useEffect hooks for smooth scroll, intersection observer, parallax, and body fade-in
       - Import all sections and components currently imported in App.jsx
       - Export as default

    3. Update `src/App.jsx`:
       - Import BrowserRouter, Routes, Route from react-router-dom
       - Keep ThemeProvider wrapper (global state must remain above router)
       - Inside ThemeProvider, add BrowserRouter wrapper
       - Inside BrowserRouter, add Routes with:
         - Route path="/" element={<Portfolio />}
         - Route path="/blog" element={<div>Blog Index Placeholder</div>}
         - Route path="/blog/:slug" element={<div>Blog Post Placeholder</div>}
         - Route path="*" element={<div>404 Not Found</div>} (catch-all)
       - Remove all section imports (they now live in Portfolio.jsx)
       - Import Portfolio from './pages/Portfolio'

    Structure should be:
    ```jsx
    <ThemeProvider>
      <BrowserRouter>
        <Routes>
          <Route path="/" element={<Portfolio />} />
          <Route path="/blog" element={...} />
          <Route path="/blog/:slug" element={...} />
          <Route path="*" element={...} />
        </Routes>
      </BrowserRouter>
    </ThemeProvider>
    ```

    Do NOT add any navigation Link components yet - that's Plan 02.
    The placeholders are intentionally minimal divs - Plan 03 creates proper components.
  </action>
  <verify>
    Run `npm run dev` and verify:
    1. http://localhost:5173/ shows the portfolio (all sections visible)
    2. http://localhost:5173/blog shows "Blog Index Placeholder"
    3. http://localhost:5173/blog/test-post shows "Blog Post Placeholder"
    4. http://localhost:5173/random-url shows "404 Not Found"
    5. Theme toggle still works on portfolio
    6. Anchor links (#about, #projects) still work via smooth scroll
  </verify>
  <done>
    All four routes render appropriate content. Portfolio functionality is identical to before (theme, smooth scroll, animations, parallax).
  </done>
</task>

</tasks>

<verification>
1. `npm ls react-router-dom` shows installed version
2. `npm run build` succeeds without errors
3. Routes respond correctly to direct URL access
4. Portfolio preserves all existing functionality
</verification>

<success_criteria>
- react-router-dom installed in dependencies
- BrowserRouter wraps the application inside ThemeProvider
- Four routes defined: /, /blog, /blog/:slug, *
- Portfolio sections render at root path with all effects working
- Direct navigation to /blog and /blog/:slug shows placeholder content
</success_criteria>

<output>
After completion, create `.planning/phases/06-routing-foundation/06-01-SUMMARY.md`
</output>
